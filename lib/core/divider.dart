import 'package:flutter/material.dart';

class DividerTiles extends StatelessWidget {
  const DividerTiles({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Divider(
      thickness: 1,
      color: Color(0xFFFAFAFA),
    );
  }
}
